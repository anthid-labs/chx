# chx-cli

Installs the `chx` command.

```bash
cargo install chx-cli
CLICKHOUSE_URL=http://default:@localhost:8123/analytics chx migrate run
```

See the [repository README](https://github.com/anthid-labs/chx).
